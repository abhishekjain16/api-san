{
  "name": "apiradar",
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate && bundle exec rake db:setup_sample_data"
  },
  "env": {
    "HEROKU_APP_NAME": {
      "required":true
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [

  ]
}
