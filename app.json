{
  "name": "apiradar",
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate && bundle exec rake db:purge_all_data_and_add_sample_data"
  },
  "env": {
    "HEROKU_APP_NAME": {
      "required":true
    },
    "RACK_ENV": "staging",
    "RAILS_ENV": "staging",
    "SECRET_KEY_BASE":{
      "generator":"secret"
    }
  },
  "formation": {
  },
  "addons": [

  ],
  "buildpacks": [

  ]
}
